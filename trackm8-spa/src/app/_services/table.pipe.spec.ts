/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TablePipe } from './table.pipe';

describe('Pipe: Tablee', () => {
  it('create an instance', () => {
    let pipe = new TablePipe();
    expect(pipe).toBeTruthy();
  });
});
